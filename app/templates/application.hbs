<a {{action "signIn" "facebook"}} class="btn">FB</a>

{{#if session.isAuthenticated}}
  <span>Currently logged in as {{session.currentUser.displayName}} with
    {{session.provider}} - <a {{action "signOut"}}>Sign out</a></span>
{{/if}}

{{outlet}}
